<% provide :title, t(".title") %>
<% provide :button_text, t(".edit_category") %>
<% provide :url_path, admin_category_path(@category) %>
<h1><%= t ".title" %></h1>

<div class="container">
  <dov class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= render "form" %>
    </div>
  </dov>
</div>
